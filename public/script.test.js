const deps = require("./script.js");

describe("Game", () => {
  
  it("can be constructed", () => {
    const game = new deps.Game();    
    expect(game).toBeDefined();
  });
  
  it("can ", () => {
    const game = new deps.Game();    
    expect(game).toBeDefined();
  });
  
});
